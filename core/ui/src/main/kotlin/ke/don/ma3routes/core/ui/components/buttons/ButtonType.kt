package ke.don.ma3routes.core.ui.components.buttons

/**
 * Defines the available types of buttons in the Ma3routes design system.
 */
enum class ButtonType {
    Primary,
    Secondary,
    Tertiary,
    Outlined,
    Danger,
}
